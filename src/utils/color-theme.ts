import { vars } from "nativewind";

export const themes = {
  light: vars({
    "--color-main-background-default": "#F5F6F7",
  }),
  dark: vars({
    "--color-main-background-default": "#0D0D0D",
  }),
};